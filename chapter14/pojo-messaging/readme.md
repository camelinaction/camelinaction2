Chapter 14 - Routing with annotations
========================================

This example shows you how to use annotations for routing.

To compile and run this example, execute the following command:

    mvn clean test

